Learn by heart! since this is foundation!

========== Data: 
```
8 data type (chu so, dung sai) => 8 Wrapper Class
      char => Character
      int => Integer
      byte => Byte
      short => Short
      long => Long
      float => Float
      double => Double

```

======== Collections: 
```
List, ArrayList, Map (HashMap), Tree
```
========= OOP
``` 
Encapsulation : problem {person.age = -10 (o ham main)} solution{...setAge()}
Polymorphism: khi runtime moi that su new object gi . Vidu WebDriver, ChromeDriver ...
Abstraction:
Inheritance:
2 Interfaces: Compatable, Comparator
Design Pattern: Strategy, Builder
```
========= Exception
```
Co 2 loai :
  Error: ko the handle , thuong la loi hardware
  Exception: checked exception (@Compile time), unchecked exception(@runtime)
  
```
========= File
```
FileInputStream fis = new FileInputStream(String absolutedFilePath);
InputStreamReader isr = new InputStreamReader(fis);
BufferReader br = new BufferReader(isr);
String currentLine = br.readLine();

FileOutputStream fos = new FileOutputStream(String absolutedFilePath);
OutputStreamWriter osw = new OutputStreamWriter(fos);
BufferWriter bw = new BufferWriter(osw);
bw.write(String str);
bw.newLine();


```


